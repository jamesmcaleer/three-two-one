<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Room - 3 2 1!</title>
    <script src="room.js"></script>
    <link href="style.css" rel="stylesheet" type="text/css">
</head>
<body>
    <label class="titleLabel3">3</label>
    <label class="titleLabel2">2</label>
    <label class="titleLabel1">1</label>
    <label class="titleLabelExc">!</label>

    

    <br>
    <br>
    
    <h1>GAME PIN</h1>
    <button class="room" onclick="copyLink()">
        <h1 id="roomCode"> <%= room.code %> </h1>
        <h4 id="copyMessage">[click to copy link]</h4>
    </button>
    
    <div class="players">
        <label id="playerOne"></label>
        <label id="playerTwo"></label>
    </div>

    <br>
    <label>input word (3 - 15 characters)</label>
    <br>
    <input type="text" id="textInput" minlength="3" maxlength="15" size="10" />
    <button id="submit" onclick="sendWord()">➤</button>

    <div id="allWords">

    </div>

    <% for(let i = room.pubWords.length - 1; i >= 0; i--){ %>
        <ul> <%= room.pubWords[i].join(" ") %> </ul>
    <% }; %>

</body>
</html>